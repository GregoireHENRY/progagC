#ifndef MATH_H
#define MATH_H

#include <math.h>

double unitx(double, double, double);
double unity(double, double, double);
double unitz(double, double, double);
double norm(double, double, double);

#endif
